# Optional: external API (default = same Next app on 3000 / Vercel — /v1/* rewrites to /api/*)
# TRIPGENT_API_URL=http://127.0.0.1:3000
# ADMIN_API_KEY=

# Optional: browser CRUD when API is on another origin only
# NEXT_PUBLIC_TRIPGENT_API_URL=
# NEXT_PUBLIC_ADMIN_API_KEY=
